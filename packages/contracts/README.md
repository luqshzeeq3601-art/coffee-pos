# Shared contracts boundary

This package is reserved for versioned TypeScript contracts shared by the POS, Admin, and KDS applications.

P1-T01 creates the package boundary only. Business schemas and public API contracts require their own reviewed task.
